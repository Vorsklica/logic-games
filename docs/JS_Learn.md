📗 Журнал навчання JavaScript

Тема заняття

Перехід від роботи безпосередньо з HTML до архітектури, заснованої на моделі даних.

Вивчено
вкладені цикли;
координатна адресація елементів;
dataset;
classList.add();
classList.remove();
побудова двовимірних масивів;
розділення логіки гри та її відображення.
Основний висновок

Екран не повинен бути джерелом інформації.

Стан гри зберігається в оперативній пам'яті (масив board), а HTML лише відображає цей стан.


10.07.2026

До журналу навчання JavaScript
Нові теми
цикл for...of;
масиви координат (напрямків);
ранній вихід із функції через return;
розділення функцій за відповідальністю (модифікація, відображення, аналіз).

Головний висновок заняття
Один і той самий алгоритм можна реалізувати по-різному. Використання масиву напрямків дозволило зробити код коротшим, зрозумілішим і легко розширюваним.

